# ADR-004: deterministic and live diagnosis providers

Status: accepted. Offline development and CI use a deterministic evidence mapper. A live provider can implement the same structured contract without leaking SDK types.
